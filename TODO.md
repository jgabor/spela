# TODO

## ⇉ Degraded

- [ ] **GUI DLL Catalog parity** — `DLLCatalogPane.svelte` is a thin stub; TUI has full library inventory + deployment matrix + update-all

## ⇢ Annoying

- [ ] **Frontend-owned option/DLL labels** — DLSS presets and DLL type labels live in the frontend while semantics come from the backend; promote them to catalog metadata if option churn increases (HEALTH.md Audit 9)
